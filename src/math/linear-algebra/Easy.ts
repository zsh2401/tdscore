import { MixedNumber } from "../../DSNumber";

export type Martix = MixedNumber[][];
export type Vector2 =
    [
        [MixedNumber],
        [MixedNumber]
    ];
export type Vector3 =
    [
        [MixedNumber],
        [MixedNumber],
        [MixedNumber],
    ]
export type Vector = MixedNumber[];
export function mul(a: Martix, b: Martix): Martix {
    return [];
}
export function det(m: Martix) {

}