import "ts-jest"
import { DSMath, DSNumber } from "../../../src";
import sin from "../../../src/math/evo/sin"
describe("s", () => {
    // it("The value of evo/sin(x) equals to Math.sin(x) in a continual x. ", () => {
    //     const MIN = -10;
    //     const MAX = 10;
    //     const DX = 0.01;
    //     for (let i = MIN; i < MAX; i += DX) {
    //         const jsResult = Math.sin(i);
    //         const evoResult = sin(DSNumber.valueOf(i));
    //         expect(evoResult.closeTo(jsResult)).toBeTruthy();
    //     }
    // });
    it("",()=>{
        
    })
    // it("Performance Test", () => {
    //     let start = new Date().getTime();
    //     sin(DSNumber.valueOf(1000));
    //     let used = new Date().getTime() - start;
    //     console.log(used);

    //     start = new Date().getTime();
    //     Math.sin(1000)
    //     used = new Date().getTime() - start;
    //     console.log(used);
     
    //     start = new Date().getTime();
    //     DSMath.sin(1000)
    //     used = new Date().getTime() - start;
    //     console.log(used);
    // })
})